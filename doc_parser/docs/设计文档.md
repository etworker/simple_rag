# doc_parser 设计文档

> 通用文档解析库 — PDF/Word → 结构化段落 + 表格

---

## 1. 设计目标

把企业常见格式的管理制度文档（PDF / Word）解析为**带定位信息的结构化数据**，为下游的向量检索、版本对比、冲突检测提供高质量输入。

核心难点与解法：

| 难点 | 解法 |
|------|------|
| 页眉/页脚/水印噪声 | 按页面坐标百分比界定页眉页脚区域，区域内的文字行自动过滤 |
| 表格文字混入正文流 | 第一遍提取表格区域坐标，第二遍拼正文时排除这些区域 |
| 跨页段落截断 | 不按页分段，而是把全文拼接成一条流，再按语义信号（空行/章节标题/句末终止符）分段 |
| 页眉页脚重复污染 | 统计每行在全文中的出现次数，超过阈值的行判定为页眉/页脚并过滤 |
| 跨页表格被截断为多个 | 启发式合并：同文件 + 页码连续 + 列数接近 + 重复表头 → 合并为一个 `Table`，跳过重复表头行 |
| 左侧 margin 编号列 | 部分 PDF 将章节编号放在左侧 margin 区域，按 x 坐标分离编号列并拼接到正文行首 |
| 空白模板表格 | 空单元格率超过阈值的表格（签到表/申请表）自动过滤 |
| 版本管理元数据行 | 修订日期/版次/版本号等独立成行的元数据，通过正则匹配从段落流中剥离 |

---

## 2. 模块结构

```
doc_parser/
  src/
    doc_parser/
      __init__.py    # 公共 API 导出: parse, Document, Paragraph, Table
      models.py      # 数据模型: Paragraph / Table / Document（含 to_dict/from_dict）
      parser.py      # 配置(DEFAULT_CONFIG) + 统一入口 parse / extract_pdf / extract_docx
      _pdf.py        # PDF 分发器: 预扫描 + 智能后端选择 select_backend + extract_pdf
      _docx.py       # Word 解析（python-docx）
      _text.py       # 文本分段 + 章节识别 + 数字断字修复（PDF / DOCX 共享）
      _tables.py     # 表格清洗 / 过滤 / 跨页合并（PDF / DOCX 共享）
      backend/       # PDF 解析后端插件包（懒加载）
        __init__.py      # 注册表 BACKENDS + load_backend() + extract()
        bk_pdfplumber.py # pdfplumber 后端（默认，轻量文本+表格）
        bk_mineru.py     # MinerU VLM/OCR 后端（扫描件/复杂版面）
        bk_docling.py    # Docling TableFormer 后端（深度学习表格识别）
  scripts/
    install_deps.py  # 依赖安装：自动检测 GPU → 安装匹配的 torch 构建
  tests/
    test_basic.py
    test_parser.py
    test_merge_cross_page_tables.py
    test_noise_line.py
    test_fixes.py    # 修复回归测试（数字断字/章节误判/后端选择）
  pyproject.toml
```

后端插件架构：每个后端在 `backend/` 下独立模块，统一暴露
`extract_pdf_with_<backend>(filepath, config)`，经 `BACKENDS` 注册表懒加载
（import 本包不引入 docling/torch 等重依赖）。pdfplumber 为内置默认，
mineru / docling 为可选依赖（`pyproject.toml` 的 `mineru` / `docling` extra）。

---

## 3. 数据模型

解析结果统一为 `Document` 对象，包含 `paragraphs`（段落列表）和 `tables`（表格列表）。
每个段落和表格都携带**定位信息**（页码、章节号、章节标题）和**来源信息**（源文件名、原始块顺序）。

### 3.1 Paragraph

```python
@dataclass
class Paragraph:
    text: str  # 段落正文
    page: int = 0  # 起始页码（Word 统一为 0）
    page_end: int = 0  # 结束页码（跨页段落时 > page）
    chapter: str = ""  # 章节号，如 "2.3"
    chapter_title: str = ""  # 章节标题，如 "备份策略"
    source_file: str = ""  # 来源文件名
    index: int = 0  # 段落序号（在同文档段落列表中的位置）
    order: int = 0  # 文档内原始块顺序（用于与表格交错渲染）
```

- `location` 只读属性：格式如 `第3-4页 / §2.3 / 备份策略`
- `to_dict()` / `from_dict()`：JSON 序列化/反序列化
- `to_markdown()`：转为 Markdown 片段（有章节号则渲染为标题）

### 3.2 Table

```python
@dataclass
class Table:
    rows: list  # 二维数组 list[list[str]]，每行一个字符串列表
    headers: list = []  # 表头（可选，为空时用 rows[0] 作表头）
    page: int = 0  # 起始页码
    page_end: int = 0  # 结束页码（跨页表格时 > page）
    chapter: str = ""  # 所属章节号
    chapter_title: str = ""  # 所属章节标题
    context_before: str = ""  # 表格前一段正文（上下文，用于理解表格含义）
    source_file: str = ""  # 来源文件名
    index: int = 0  # 表格序号
    order: int = 0  # 文档内原始块顺序
```

- `location` 只读属性：格式如 `第3-4页 / §2.1 / 风险应对`
- `to_markdown()`：转为 Markdown 表格语法

### 3.3 Document

```python
@dataclass
class Document:
    filename: str                # 源文件名
    paragraphs: list[Paragraph]  # 段落列表
    tables: list[Table]          # 表格列表

    def to_dict(self) -> dict             # 序列化为 JSON 友好的 dict
    def to_markdown(self) -> str          # 转为 Markdown（段落和表格按原始顺序交错）
    @classmethod
    def from_dict(cls, d) -> Document     # 从 dict 反序列化
```

`to_markdown()` 排序策略：
- 优先使用 `order` 字段（解析器提供的原始块顺序）
- `order=0`（旧数据）回退到 `index`
- 章节标题自动渲染为 `#` / `##` / `###`（层级按编号深度推断）
- 目录条目（末尾有页码引用）自动跳过

### 3.4 输出结构示例

以下是一个 PDF 解析后的完整输出示例：

**Python 对象：**
```python
Document(
    filename="信息技术部工作手册.pdf",
    paragraphs=[
        Paragraph(
            text="本手册适用于信息技术部所有人员，规范日常运维操作流程。",
            page=1,
            page_end=1,
            chapter="1",
            chapter_title="总则",
            source_file="信息技术部工作手册.pdf",
            index=1,
            order=1,
        ),
        Paragraph(
            text="备份策略应遵循每日全量、每周增量的原则，保留期不少于30天。",
            page=2,
            page_end=3,  # ← 跨页段落
            chapter="2.3",
            chapter_title="备份策略",
            source_file="信息技术部工作手册.pdf",
            index=5,
            order=7,
        ),
        # ... 更多段落
    ],
    tables=[
        Table(
            rows=[
                ["序号", "风险描述", "应对措施", "责任人"],
                ["1", "系统宕机", "双机热备切换", "运维组"],
                ["2", "数据丢失", "从备份恢复", "DBA"],
                ["3", "网络中断", "启用备用链路", "网络组"],
            ],
            headers=[],
            page=3,
            page_end=4,  # ← 跨页表格
            chapter="2.1",
            chapter_title="风险应对",
            context_before="主要风险及应对措施如下表所示：",
            source_file="信息技术部工作手册.pdf",
            index=1,
            order=5,
        ),
        # ... 更多表格
    ],
)
```

**序列化为 JSON（`doc.to_dict()`）：**
```json
{
  "filename": "信息技术部工作手册.pdf",
  "paragraphs": [
    {
      "text": "本手册适用于信息技术部所有人员，规范日常运维操作流程。",
      "page": 1,
      "page_end": 1,
      "chapter": "1",
      "chapter_title": "总则",
      "source_file": "信息技术部工作手册.pdf",
      "index": 1,
      "order": 1
    },
    {
      "text": "备份策略应遵循每日全量、每周增量的原则，保留期不少于30天。",
      "page": 2,
      "page_end": 3,
      "chapter": "2.3",
      "chapter_title": "备份策略",
      "source_file": "信息技术部工作手册.pdf",
      "index": 5,
      "order": 7
    }
  ],
  "tables": [
    {
      "rows": [
        ["序号", "风险描述", "应对措施", "责任人"],
        ["1",   "系统宕机", "双机热备切换", "运维组"],
        ["2",   "数据丢失", "从备份恢复",   "DBA"],
        ["3",   "网络中断", "启用备用链路", "网络组"]
      ],
      "headers": [],
      "page": 3,
      "page_end": 4,
      "chapter": "2.1",
      "chapter_title": "风险应对",
      "context_before": "主要风险及应对措施如下表所示：",
      "source_file": "信息技术部工作手册.pdf",
      "index": 1,
      "order": 5
    }
  ]
}
```

**转为 Markdown（`doc.to_markdown()`）：**
```markdown
# 1 总则

本手册适用于信息技术部所有人员，规范日常运维操作流程。

## 2.1 风险应对

主要风险及应对措施如下表所示：

| 序号 | 风险描述 | 应对措施 | 责任人 |
| --- | --- | --- | --- |
| 1 | 系统宕机 | 双机热备切换 | 运维组 |
| 2 | 数据丢失 | 从备份恢复 | DBA |
| 3 | 网络中断 | 启用备用链路 | 网络组 |

## 2.3 备份策略

备份策略应遵循每日全量、每周增量的原则，保留期不少于30天。
```

**字段说明汇总：**

| 字段 | 类型 | 含义 | 示例 |
|------|------|------|------|
| `text` | str | 段落正文 | `"备份策略应遵循..."` |
| `page` | int | 起始页码 | `2` |
| `page_end` | int | 结束页码 | `3`（跨页时 > page） |
| `chapter` | str | 章节号 | `"2.3"` |
| `chapter_title` | str | 章节标题 | `"备份策略"` |
| `source_file` | str | 来源文件名 | `"信息技术部工作手册.pdf"` |
| `index` | int | 同类序号 | 段落列表中的位置 |
| `order` | int | 原始块顺序 | 用于段落与表格交错 |
| `rows` | list[list[str]] | 表格数据 | `[["序号", "风险"], ["1", "宕机"]]` |
| `headers` | list | 表头 | `[]`（为空时用 rows[0]） |
| `context_before` | str | 表格前正文 | `"主要风险及应对措施如下表所示："` |

---

## 4. PDF 解析流程（`extract_pdf`）

`extract_pdf` 是**分发器**：根据配置选择后端，实际解析由 `backend/` 下的后端模块完成。

```
extract_pdf(filepath, config)
  backend = cfg["backend"]          # "auto"(默认) / "mineru" / "docling" / "pdfplumber"
  "auto" → select_backend() 预扫描决定（扫描件→mineru，正常→pdfplumber）
  分发到:
    bk_pdfplumber.extract_pdf_with_pdfplumber()  # 两遍遍历（下方详解）
    bk_mineru.extract_pdf_with_mineru()          # MinerU do_parse → content_list.json
    bk_docling.extract_pdf_with_docling()        # Docling TableFormer → iterate_items
```

MinerU / Docling 后端把各自的块输出（MinerU content_list、Docling TableItem/
SectionHeaderItem/TextItem）转换为统一的 `Paragraph` / `Table`，然后**复用**
`_merge_cross_page_tables` / `_filter_template_tables` / `detect_chapter` /
`normalize_number_spacing` 等共享后处理逻辑，保证各后端输出结构一致。

### pdfplumber 后端：两遍遍历

采用**单次打开 PDF、两遍遍历**策略：

#### 第一遍：收集 + 提取表格

```
for each page:
    1. 提取表格（pdfplumber.find_tables）+ 记录表格区域坐标
    2. 收集所有文字行（words → lines，按 y 坐标聚合）
    3. 过滤页眉/页脚区域内的行（top < page_height * header_margin_pct% 或 bottom > page_height * (100 - footer_margin_pct)%）
    4. 分离左侧 margin 编号列（x < margin_number_x 且匹配编号正则的 word）
```

统计所有行在全文中的出现频率，出现次数超过 `repeat_line_threshold_pct%` 的行判定为页眉/页脚重复行。

#### 第二遍：拼接正文流 + 语义分段

```
for each page:
    排除表格区域和重复行后的文字行
    → 拼接到全文流（带页码边界信息）

在全文流上调用 _split_stream 分段：
    分割信号（优先级从高到低）：
    1. 章节标题行前断开
    2. 连续空行
    3. 句末终止符 + 段落已达一定长度（>= sentence_break_min_length）
    4. 长度上限强制断开（>= max_paragraph_length）

分段后通过 _find_page 反查每段对应的页码
```

#### 后处理（各后端共享）

```
_normalize_number_spacing  # 数字断字修复："28. 55 万" → "28.55"（_text.py）
_filter_template_tables    # 过滤空白模板表格
_merge_cross_page_tables   # 合并跨页连续表格
_assign_table_chapters     # 为表格分配最近的章节信息
```

---

## 5. Word 解析流程（`extract_docx`）

使用 `python-docx`，通过 `_iter_docx_blocks` 按 OOXML body 顺序交替遍历段落和表格：

```python
def _iter_docx_blocks(doc):
    for child in doc.element.body.iterchildren():
        if isinstance(child, CT_P):
            yield "paragraph", DocxParagraph(child, doc)
        elif isinstance(child, CT_Tbl):
            yield "table", DocxTable(child, doc)
```

**关键设计：按 body 块顺序遍历**，而非分别遍历 `doc.paragraphs` 和 `doc.tables`。
后者会让所有表格错误地继承最后一个章节号，因为章节检测是在段落遍历时推进的。

- **段落**：过滤空文本 → 先检测章节标题（短标题免长度过滤）→ 构建 `Paragraph`
- **表格**：`row.cells` 提取文本 → `_clean_table` 清理空行空列 → 构建 `Table`
- **`order` 字段**：记录每个块在文档中的原始位置，用于 `to_markdown()` 交错渲染
- **后处理**：与 PDF 路径一致的 `_filter_template_tables` + `_assign_table_chapters`

Word 文档无页码概念，`page` 字段统一为 0。

---

## 6. 跨页表格合并（`_merge_cross_page_tables`）

启发式合并条件（同时满足）：

1. 同一 `source_file`
2. 页码连续（后续表格的 `page` == 前一表格的 `page_end` + 1）
3. 列数接近（差异 ≤ 1）

> 表头相似度**不参与合并判定**——仅用于追加阶段决定是否跳过重复表头行。
> （历史版本曾把"首行相似度 ≥ 0.85"作为合并前置条件，因会漏合并表头被裁切/变形的续表而移除。）

合并后：
- 保留首张表格的 `headers` / `page` / `chapter`
- 续页的数据行追加到首张的 `rows` 末尾（`_append_rows_skip_dup_header`）
- 续页第 0 行若与表头 Token Jaccard 相似度 ≥ 0.85 → 跳过（重复表头）
- 列数对齐：短的补空，长的截断
- 更新 `page_end` = 末页页码

---

## 7. 章节检测（`_detect_chapter`）

通过正则匹配识别章节标题。默认模式：

| 模式 | 匹配示例 |
|------|----------|
| `^(\d+\.\d+\.\d+)\s+(.+)` | `1.2.3 安全管理要求` |
| `^(\d+\.\d+)\s+(.+)` | `2.3 备份策略` |
| `^(\d+)\s+(.+)` | `3 总则` |
| `^第\s*(\d+)\s*[章节]\s*(.+)` | `第3章 运维管理` |
| `^第\s*[一二三四五六七八九十百千]+\s*[章节]\s*(.+)` | `第六章 信息安全管理` |
| `^[一二三四五六七八九十]+、\s*(.+)` | `一、生产运行情况` |
| `^（[一二三四五六七八九十]+）\s*(.+)` | `（一）适用范围` |

约束与防误判：
- 文本长度超过 `max_chapter_title_length`（默认 80）的不视为标题
- 单纯数字编号 `^\d+` 的标题长度超过 `single_number_title_max_length`（15）或值超过
  `single_number_max_value`（999）→ 视为正文（年份/长行）
- **量词前缀表**（`_NUM_UNIT_PREFIXES`：万人次/月份/万架次/万小时…）：编号后紧跟统计
  量词（`3 万人次`、`6 月份`）判为统计数字而非章节，真实标题 `2 职责分工` 保留

## 7.5 数字断字修复（`normalize_number_spacing`）

部分 PDF 文本层把数字按字符拆成独立 text run（字符间距与词间距无几何区分度），
所有文本后端（pdfplumber / docling）都会断字，几何修复不可行 → 后处理正则：

- `(\d)\.\s+(\d)` → `\1.\2`：`28. 55 万小时` → `28.55`（小数被拆）
- `(\d+)\s+(\d+)\.(?=\s*(?:万|亿|％|%|吨|米|公里))` → `\1.\2`：`57 10. 万架次` → `57.10`

默认开启（`normalize_number_spacing: True`），在全文字段级后处理中对每个段落文本应用。

---

## 8. 元数据行剥离（`noise_line_patterns`）

独立成行的版本管理元数据（如"修订日期：2026-05-08"）会被从段落流中剥离，避免被并入相邻正文造成句子拼接。默认匹配模式：

```
^修订日期\s*[：:]\s*\S+$
^发布日期\s*[：:]\s*\S+$
^修订时间\s*[：:]\s*\S+$
^\d{4}[-./]\s*\d{1,2}[-./]\s*\d{1,2}$
^版\s*次\s*[：:]\s*\S+$
^版本号\s*[：:]\s*\S+$
^(?:R\d{2,}|版本\s*\S+)\s*$
^修订次数\s*[：:]\s*\d+\s+\S+\s+页码
修订日期\s*[：:]\s*\d{4}-\d{1,2}-\d{1,2}\s*$
```

所有模式均可通过 `config['extract']['noise_line_patterns']` 覆盖。

---

## 9. 智能后端选择（`select_backend`，`backend: "auto"`）

`auto` 模式先对 PDF 做快速预扫描（采样前 `scan_sample_pages` 页，默认 5 页，< 1s），
根据文档特征自动选择后端；MinerU/Docling 未装时自动降级到 pdfplumber：

| 检测项 | 条件 | 选择 | 理由 |
|--------|------|------|------|
| 扫描件 | 平均文字 < `scan_text_per_page_threshold`（50）字/页 | MinerU | 需要 OCR |
| 扫描件 | 大图片覆盖 > `scan_large_image_ratio`（50%）**且**平均文字 < `scan_image_text_per_page_threshold`（300）字/页 | MinerU | 图片型 PDF |
| 无框线表格 | 有绘图对象但 pdfplumber 提取不到表格 | MinerU | 表格被漏掉 |
| 无框线表格 | 文本含表头关键词 + 列对齐特征 | MinerU | 疑似表格 |
| 正常文档 | pdfplumber 表格提取正常 | pdfplumber | 速度快 |

> 大图片判定与文本量**联合**（第三行）：公文红头/印章/配图也是大图，但文本量充足，
> 不会误判为扫描件。仅当"大图覆盖高 **且** 文本仍少"同时成立才走 MinerU。

---

## 10. 配置项

| 配置键 | 默认值 | 说明 |
|--------|--------|------|
| `header_margin_pct` | 8 | 页眉区域占页面高度的百分比 |
| `footer_margin_pct` | 8 | 页脚区域占页面高度的百分比 |
| `repeat_line_threshold_pct` | 30 | 行在全文出现次数超过此百分比 → 判为页眉/页脚 |
| `min_paragraph_length` | 10 | 段落最小字符数，短于此值的丢弃 |
| `max_paragraph_length` | 600 | 段落最大字符数，超长时强制断开 |
| `max_chapter_title_length` | 80 | 章节标题最大长度 |
| `sentence_break_min_length` | 40 | 句末断句的最小段落长度 |
| `chapter_patterns` | (见上文) | 章节标题正则列表 |
| `noise_line_patterns` | (见上文) | 元数据行剥离正则列表 |
| `margin_number_x` | 130 | 左侧编号列的 x 坐标阈值（0 = 禁用） |
| `margin_number_pattern` | `^(?:\d+(?:\.\d+)*\|[A-Z])$` | 编号列中 word 的匹配正则 |
| `table_empty_cell_threshold` | 0.6 | 空单元格率超过此值的表格过滤（1.0 = 禁用） |
| `table_empty_placeholders` | `["□", "☐", "○", "——"]` | 视为空单元格的占位符 |
| `normalize_number_spacing` | True | 数字断字后处理（`28. 55` → `28.55`） |
| `backend` | `"auto"` | PDF 后端：`"auto"` / `"pdfplumber"` / `"mineru"` / `"docling"` |
| `mineru_backend` | `"auto"` | MinerU 引擎：`"vlm"`(GPU) / `"pipeline"`(CPU) / `"auto"`(自动检测) |
| `mineru_output_dir` | `""` | MinerU 输出目录（空=临时目录） |
| `mineru_timeout` | `600` | MinerU 解析超时秒数 |
| `mineru_start_page` | 0 | MinerU 起始页（0-based，None=全部） |
| `mineru_end_page` | None | MinerU 结束页（0-based，None=全部） |
| `docling_start_page` | None | Docling 起始页（1-based，None=全部） |
| `docling_end_page` | None | Docling 结束页（1-based 含端点，None=全部） |
| `docling_ocr` | False | Docling 是否 OCR（仅扫描件需要） |
| `docling_device` | `"auto"` | Docling 推理设备：`"auto"` / `"cpu"` / `"cuda"` / `"mps"` |
| `scan_sample_pages` | 5 | 智能后端预扫描页数（0=全部） |
| `scan_text_per_page_threshold` | 50 | 扫描件判定：平均每页文字低于此值 |
| `scan_large_image_ratio` | 0.5 | 大图片覆盖页面比例阈值 |
| `scan_image_text_per_page_threshold` | 300 | 大图判定联合文本阈值（文本低于此值才判扫描件） |
| `scan_drawings_no_tables_ratio` | 0.4 | 有绘图线但无表格的页数比例阈值 |
| `scan_low_table_rate` | 0.3 | 低表格提取率阈值 |
| `table_keyword_list` | (13 个) | 无框线表格检测的表头关键词 |
| `table_keyword_hit_ratio` | 0.5 | 关键词命中比例阈值 |
| `table_aligned_line_ratio` | 0.3 | 列对齐行命中比例阈值 |
| `y_tolerance` | 3 | word 行聚合的 Y 坐标容差（pt） |

配置传入方式：`parse(filepath, config={"extract": {...}})`，与默认配置深拷贝后合并。
