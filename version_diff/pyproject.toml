[project]
name = "version-diff"
version = "0.1.0"
description = "文档差异检测引擎 — 语义配对 + LLM 矛盾判断"
requires-python = ">=3.10"
dependencies = [
    "doc-parser",
    "llm-chat",
    "sentence-transformers>=3.3.0",
    "faiss-cpu>=1.7.4",
    "numpy>=1.24.0",
    "loguru>=0.7.3",
]

[tool.uv.sources]
doc-parser = { path = "../doc_parser", editable = true }
llm-chat = { path = "../llm_chat", editable = true }

[project.optional-dependencies]
dev = ["pytest>=7.0"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/version_diff"]

[tool.hatch.build.targets.wheel.force-include]
"src/version_diff/prompts" = "version_diff/prompts"

[tool.ruff]
line-length = 120

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B", "C4", "SIM", "FURB", "RUF"]
ignore = ["BLE001", "S110", "ASYNC230", "RUF001", "RUF002", "RUF003", "E402"]
